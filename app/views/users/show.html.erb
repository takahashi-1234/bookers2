<div class="container mt-3">
  <div class="row">
    <div class="col-lg-4">
      <%= render 'users/list',user:@user ,book:@book %>
    </div>
    <div class="col-lg-8">
      <h1>Books</h1>
      <table class="table table-hover">
        <thaed>
          <th></th>
          <th>Title</th>
          <th>Opinion</th>
        </thead>
        <% @user.books.each do |book| %>
        <tr>
          <td><%= attachment_image_tag book.user,:profile_image,fallback:"no_image.jpg",size:"60x72" %></td>
          <td><%= link_to book_path(book.id) do %><%= book.title %><% end %></td>
          <td><%= book.body %></td>
        </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>